server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/abc/abc #.git 后面的.git可以不需要
          #clone-on-start: true #表示是否从远程下载到server，不需要每次请求都请求到远程git仓库
          search-paths:
          - foo # foo路径
          - bar # bar路径